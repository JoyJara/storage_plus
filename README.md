Para ejecutar la versión móvil del proyecto entra a la ruta frontend/mobile y ejecuta el comando "npx expo start".

Si quieres acceder al CRUD de la aplicación desde la web, tienes que correr el backend localmente, en este repositorio entra a la ruta backend/ y ejecuta el comando "npm run dev"

RUTA DEL CRUD: http://localhost:3000/api/docs/
